# MAD Analytics — Python dependencies

# Core ML / stats
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.2.0
statsmodels>=0.14.0

# Explainability (optional but recommended)
shap>=0.45.0

# Change-point detection (optional)
ruptures>=1.1.9

# Model persistence
joblib>=1.3.0

# Validation
pydantic>=2.6.0

# API server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# DB connection (for training script only)
# Upper bound is deliberate: SQLAlchemy 2.1.0 changed the DEFAULT dialect for
# a bare "postgresql://" URL from psycopg2 to psycopg v3 (confirmed live,
# 2026-09-25 production incident) -- this service only ships psycopg2-binary,
# not psycopg. utils/db.py now also pins the driver explicitly in the URL
# itself as an independent guard, but this cap stops an unpinned future
# SQLAlchemy release from silently changing other defaults the same way.
sqlalchemy>=2.0.0,<2.1.0
psycopg2-binary>=2.9.9

# Web scraping (Instagram profiles, BookMyShow, etc.)
playwright>=1.44.0
playwright-stealth>=1.0.6
apify-client>=3.0.0

# Google Trends (popularity scoring)
pytrends>=4.9.2